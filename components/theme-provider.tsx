"use client";

// Theme system has been permanently removed.
// The site now uses a fixed dark design only.
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
